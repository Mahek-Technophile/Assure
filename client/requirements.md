## Packages
framer-motion | Page transitions and complex animations
recharts | Data visualization for admin dashboards
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes efficiently

## Notes
The backend is mocked, so we will simulate some delays in the UI to make it feel realistic.
Authentication state will be managed client-side for this demo since there is no /api/me endpoint in the provided schema.
